# AI-Reasoning-Lab

AI-Reasoning-Lab — это фреймворк с открытым исходным кодом для проектирования, выполнения и анализа сложных, состоянийных, многошаговых задач на рассуждение с целью глубокой оценки когнитивных способностей продвинутых языковых моделей. Платформа позволяет оценивать не только конечный ответ LLM, но и весь процесс размышлений-по-шагам, включая скорость генерации и «болтливость».

## Проблема: "Слепое пятно" в оценке LLM

Современные инструменты оценки LLM, такие как lm-evaluation-harness или OpenAI Evals, отлично справляются с бенчмаркингом моделей на стандартных задачах. Однако они в основном оценивают точность конечного ответа. Это создает критическое "слепое пятно": модель может прийти к правильному выводу, используя ошибочную логику, или просто "угадать" ответ, и система оценки зафиксирует успех. Мы измеряем не истинную способность к рассуждению, а лишь сложную форму сопоставления с образцом. Для создания действительно надежных и интеллектуальных систем нам нужны инструменты, которые могут заглянуть "под капот" и оценить сам процесс рассуждения.

## Наше решение: Оценка процесса, а не только результата

AI-Reasoning-Lab смещает фокус с оценки конечного ответа на валидацию всей цепочки рассуждений. Вместо простых пар "промпт-ответ" наш фреймворк позволяет определять сложные, многошаговые задачи, где вывод одного шага становится входом для следующего. Это позволяет детально анализировать, как модель строит свою логику, выявлять ошибки в промежуточных шагах и по-настоящему измерять ее когнитивные способности.

## Ключевые возможности

- **Потоковый режим с трассировкой чанков** — детальное профилирование времени генерации каждого токена (⏱️ Chunk \#N через … с)
- **Многошаговая и состоянийная оценка** — Создавайте задачи, где состояние (контекст) сохраняется и передается между шагами, имитируя реальные процессы решения проблем и Chain-of-Thought (CoT)
- **Оценка промежуточных шагов** — Применяйте метрики не только к финальному результату, а к выводу на любом шаге цепочки рассуждений
- **Автоматический менеджер GGUF-моделей** — файл scripts/create_model.py скачивает *.gguf, создаёт Modelfile и регистрирует модель в Ollama
- **Плагинная архитектура моделей** — Легко подключайте новые модели через адаптеры. MVP поддерживает локальные модели transformers и API (например, Gemini), а архитектура позволяет легко добавлять новые
- **Конфигурация через .env** — все параметры запуска, генерации и стрима задаются переменными окружения, без правки кода
- **Расширяемая система метрик** — Используйте встроенные метрики (exact_match, regex_match) или легко добавляйте свои собственные. Включает поддержку методологии "LLM-как-судья" для качественной оценки, Wilson Score, Verbosity Index
- **Детализированные результаты** — Получайте подробные логи каждого шага в формате JSONL, создавая полный "след доказательств" процесса рассуждения модели для последующего анализа, Markdown-отчёты, heat-map визуализации

## Что нового (v0.2.0)

| Категория | Изменения |
| :-- | :-- |
| Стриминг | -  Поддержка stream=True с логами чанков.<br>-  Лимит max_chunks, завершение по finish_reason или стоп-токенам. |
| GGUF менеджер | -  Корректная обработка URL без ?download=.<br>-  Повторный запуск не скачивает файл повторно.<br>-  Таймаут ollama create увеличен до 10 мин. |
| Конфигурация | -  Новые .env-флаги: THINK, STREAM, MAX_CHUNKS, STOP, MAX_TOKENS. |
| Документация | -  Полный README с quick-start Ollama и настройкой стрима. |
| Диагностика | -  Логирование промпта и опций генерации перед каждым запросом. |
| **LLM-Судьи** | -  **Полноценная поддержка LLM-как-судья метрик**<br>-  **Рейтинг надежности моделей-арбитров** |

## 📚 Раздел base_logic - Базовая Логическая Платформа

### Назначение и философия

BaseLogic представляет собой фундаментальную тестовую платформу для комплексного анализа и оценки языковых моделей. Модуль построен на принципах модульности, воспроизводимости и безопасности, обеспечивая надежную основу для всех типов тестирования.

### Архитектура: Четырехуровневая система

**Уровень 1: Утилиты и Ядро (Core \& Utilities)**

```
baselogic/core/
├── config_loader.py     # Singleton управления конфигурацией
├── logger.py            # Централизованная система логирования
├── types.py             # Система типов и интерфейсов
├── enums.py             # Перечисления и константы
└── interfaces.py        # Контракты взаимодействия
```

**Уровень 2: Сервисный слой (Service Layer)**

```
├── client_factory.py    # Factory Pattern для LLM клиентов
├── llm_client.py        # Основной клиент LLM
├── base_client.py       # Базовая абстракция клиента
├── http_client.py       # HTTP клиент для API
└── safe_evaluator.py    # Безопасное выполнение кода
```

**Уровень 3: Бизнес-логика (Business Logic)**

```
├── test_runner.py       # Оркестратор тестирования (Strategy Pattern)
├── plugin_manager.py    # Система плагинов
├── progress_tracker.py  # Отслеживание прогресса
├── metrics.py           # Система метрик и статистики
└── config_validator.py  # Валидация конфигурации
```

**Уровень 4: Отчетность и анализ (Reporting Layer)**

```
├── reporter.py          # Базовый генератор отчетов с Wilson Score
├── advanced_reporter.py # Продвинутая аналитика и сегментация
├── judge_reporter.py    # Специализированные отчеты для LLM-судей
└── report_formatter.py  # Форматирование вывода
```

### Встроенные категории тестов

| ID | Назначение |
| :-- | :-- |
| t01_simple_logic | Простые дедуктивные цепочки |
| t02_instructions | Проверка строгого следования инструкциям |
| t06_mathematics | Арифметические выражения с SafeExpressionEvaluator |
| **t07_accuracy_ideal** | **Проверка точности судейства на идеальных ответах** |
| **t08_accuracy_flawed** | **Проверка точности судейства на ошибочных ответах** |
| **t09_verbosity_ideal** | **Тест устойчивости к многословности** |
| **t10_verbosity_verbose** | **Тест на переустойчивость к краткости** |
| **t11_positional_first** | **Тест позиционной предвзятости (первый вариант)** |
| **t12_positional_second** | **Тест позиционной предвзятости (второй вариант)** |

## 🏛️ Система LLM-Судей: Объективная оценка рассуждений

### Проблема: Кто оценивает оценщика?

Методология "LLM-как-судья" становится стандартом для оценки сложных, открытых задач, где нет единственного правильного ответа. Однако сами модели-судьи могут страдать от **предвзятости, непоследовательности и позиционных искажений**. AI-Reasoning-Lab впервые предоставляет комплексную систему для **оценки качества самих судей**.

### Метрики качества LLM-судей

| Метрика | Описание | Критерий оценки |
| :-- | :-- | :-- |
| **Accuracy Score** | Способность правильно различать хорошие и плохие ответы | Точность классификации на контрольных примерах |
| **Stability Score** | Постоянство оценок при повторных запросах | Консистентность результатов при многократном тестировании |
| **Positional Resistance** | Устойчивость к порядку представления вариантов | Независимость оценки от позиции ответа (первый/второй) |
| **Verbosity Resistance** | Устойчивость к длине и стилю ответа | Фокус на содержании, а не на объеме текста |
| **Format Adherence** | Соблюдение требуемого формата ответа | Процент корректно структурированных ответов |

### 🏆 Рейтинг LLM-Судей (актуальные данные)

**Последнее обновление**: 22 августа 2025

| Модель | Judge Rating | Accuracy Score | Stability Score | Positional Resistance | Verbosity Resistance |
| :-- | :--: | :--: | :--: | :--: | :--: |
| **qwen/qwen3-30b-a3b-2507** | **0.67** | 0.85 | 0.888 | 0 | 0 |
| **qwen/qwen3-4b-thinking-2507** | **0.41** | 0.20 | 0.758 | 0 | 0 |
| **deepseek/deepseek-r1-0528-qwen3-8b** | **0.15** | 0.00 | 0.000 | 0 | 0 |

> **qwen/qwen3-30b-a3b-2507** показывает наилучшие результаты в качестве модели-судьи, демонстрируя высокую точность (85%) и стабильность (88.8%) при оценке ответов.

## 🔬 Специализированный стресс-тест t_context_stress

### Проблема: "Потерянная середина" и иллюзия контекста

Многие модели заявляют о поддержке огромных контекстных окон (100K, 200K или даже 1M токенов), но на практике часто "забывают" информацию, расположенную в середине этого контекста. Этот тест разработан для проверки двух ключевых гипотез:

1. Способна ли модель в принципе найти факт в большом объеме текста?
2. Зависит ли ее способность от положения факта (в начале, середине или конце текста)?

### Методология: "Иголка в стоге сена" (Needle in a Haystack)

Методика «Needle in a Haystack» проверяет, теряет ли LLM факты в длинных контекстах (10% / 50% / 90% глубины). Тест автоматически генерирует серию задач, где:

1. "Иголка": Создается уникальный, вымышленный факт (например, "Секретный ингредиент марсианского пирога — астероидная пыль")
2. "Стог сена": Генерируется большой объем нейтрального текста заданного размера (например, 32K, 64K, 128K токенов)
3. Внедрение: "Иголка" помещается на разную глубину "стога сена" (например, на 10%, 50% и 90% от начала)
4. Проверка: Модели задается прямой вопрос по "иголке"

Отчёт содержит таблицу точности vs длина контекста и тепловую карту «✅/❌» по глубине.

## 🏆 Генератор головоломок Grandmaster

### Назначение и инновация

Grandmaster - это продвинутый процедурный генератор логических головоломок экспертного уровня, разработанный для создания уникальных задач, которые невозможно найти в обучающих данных LLM. Это ключевое решение проблемы "data contamination" в современных бенчмарках.

### Алгоритм "Гроссмейстер-Виртуоз"

Трехфазный процесс целенаправленного проектирования головоломок:

**Фаза 1: Проектирование "Каркаса Интриги"** — Выбор "экзотических" улик из сложных типов, размещение в ядре головоломки, дополнение другими сложными уликами.

**Фаза 2: Интеллектуальное Укрепление** — Итеративное добавление только сложных улик с постоянной проверкой на противоречивость.

**Фаза 3: Шлифовка и Финальный Аудит** — Пакетная минимизация и аудит качества с построением графа связей.

Процедурно создаёт уникальные логические задачи (4×4 … 8×8) с гарантией единственного решения, используя Google OR-Tools. Каждая головоломка генерируется уникально, обеспечивая защиту от "заучивания" решений и истинную оценку когнитивных способностей.

## Быстрый запуск AI-Reasoning-Lab через `run.py`

`run.py` — универсальный входной скрипт, который загружает переменные из `.env`, готовит/подтягивает модели (если включён `PREPARE_MODEL_*`), читает список тестов и моделей, стартует базовую платформу (Base Logic) и формирует Markdown-отчёт. Ниже представлен минимальный сценарий «скачай модель → создай → протести».

### 1. Подготовка конфигурации

**Добавьте в .env полную конфигурацию:**

```dotenv
# ---------- подготовка модели ----------
PREPARE_MODEL_0_ENABLED="true"
PREPARE_MODEL_0_NAME="jan-v1-4b"
PREPARE_MODEL_0_GGUF_URL="https://huggingface.co/unsloth/.../Qwen3-4B-Q4_K_M.gguf?download=true"
PREPARE_MODEL_0_SYSTEM_PROMPT="You are a helpful assistant"
PREPARE_MODEL_0_PARAMS_TEMPERATURE="0.2"
PREPARE_MODEL_0_PARAMS_NUM_CTX="2048"

# ---------- инференс ----------
BC_MODELS_0_NAME="jan-v1-4b"           # то же имя, что выше
BC_MODELS_0_PROVIDER="ollama"
BC_MODELS_0_INFERENCE_STREAM="true"
BC_MODELS_0_INFERENCE_THINK="false"
BC_MODELS_0_INFERENCE_MAX_CHUNKS="1000"
BC_MODELS_0_GENERATION_MAX_TOKENS="20"
BC_MODELS_0_GENERATION_STOP='["\n"]'

# ---------- тесты ----------
BC_TESTS_TO_RUN='["t01_simple_logic"]'
```

### 2. Альтернативный способ: ручная подготовка модели

Если предпочитаете управлять подготовкой модели вручную:

1. **Запустите менеджер:**
```bash
python scripts/create_model.py
```

2. **Убедитесь, что модель появилась:**
```bash
ollama list
```

### 3. Запуск платформы

```bash
python run.py
```

### 4. Процесс выполнения

1. **Менеджер GGUF** — скачивает файл (если нет), создает Modelfile и выполняет `ollama create jan-v1-4b -f /tmp/tmp_Modelfile`
2. **Инициализация Base Logic** — считывает `BC_TESTS_TO_RUN`, собирает плагины, поднимает LLM-клиент (Ollama)
3. **Тестовый прогон в потоковом режиме** — отправляет промпт в режиме `stream=True`; в консоли отображается прогресс:
```
⏱️ Chunk #1 через 0.23 сек
⏱️ Chunk #2 через 0.35 сек
…
✅ Модель завершила генерацию (done=True) на chunk #8
```

4. **Генерация отчёта** — сырые результаты сохраняются в `results/raw/*.json`; сводный Markdown-отчёт создается как `BASE_LOGIC_BENCHMARK_REPORT.md`

### 5. Дополнительные параметры запуска

`run.py` принимает необязательные CLI-флаги, которые переопределяют настройки из `.env`:

```bash
python run.py \
  --models jan-v1-4b,gemma:2b \
  --tests t01_simple_logic,t_context_stress \
  --stream            # то же, что BC_MODELS_*_INFERENCE_STREAM="true"
  --think             # включает think=True
  --max-chunks 500
```

### 6. Проверка результатов

После успешного запуска просмотрите сгенерированные отчёты:

```bash
# Основной бенчмарк моделей
cat BASE_LOGIC_BENCHMARK_REPORT.md | less

# Рейтинг LLM-судей (если запускались тесты судейства)
cat JUDGE_LEADERBOARD.md | less
```

В отчётах вы увидите:
- **Основная таблица лидеров** с Trust Score, точностью и временем ответа
- **Детальную статистику по категориям** тестов
- **Рейтинг надёжности LLM-судей** с метриками качества оценки

### 7. Прямое тестирование модели через HTTP API

Для быстрой проверки работоспособности модели можно использовать прямые HTTP-запросы к Ollama API:

Пример для MacOS
```bash
curl -X POST http://localhost:11434/api/chat \
  -d '{"model": "jan-v1-4b", "messages": [{"role": "user", "content": "Скажи привет"}], "stream": true}'
```
Пример для Windows
```bash
curl -X POST http://localhost:11434/api/chat -H "Content-Type: application/json" -d "{\"model\": \"jan-v1-4b\", \"messages\": [{\"role\": \"user\", \"content\": \"Скажи привет\"}], \"stream\": true}"
```

Этот способ позволяет убедиться, что модель корректно загружена и отвечает, до запуска полного тестирования через AI-Reasoning-Lab.

## 📊 Результаты тестирования: Топ-25 моделей

**Последнее обновление**: 22 августа 2025

| Ранг | Модель | Trust Score | Accuracy | Avg Time | Runs |
| :--: | :-- | :--: | :--: | :--: | :--: |
| 1 | **gemini-2.5-flash** | 0.782 | 95.5% | 2,799 мс | 22 |
| 2 | **qwen/qwen3-4b-thinking-2507** | 0.726 | 79.1% | 16,953 мс | 182 |
| 3 | **tngtech/deepseek-r1t2-chimera:free** | 0.699 | 90.0% | 10,095 мс | 20 |
| 4 | **gemini-2.5-flash-lite** | 0.657 | 88.2% | 1,346 мс | 17 |
| 5 | **jan-v1-4b** | 0.600 | 69.8% | 29,280 мс | 96 |

*Полная таблица из 25 моделей доступна в `BASE_LOGIC_BENCHMARK_REPORT.md` после запуска тестирования.*

## Основная философия

Мы считаем, что будущее оценки LLM — не в создании все более крупных монолитных бенчмарков, а в предоставлении исследователям и разработчикам мощных инструментов для создания собственных, предметно-ориентированных тестов. Наша цель — сделать experiment.yaml универсальным языком (lingua franca) для описания и обмена сложными экспериментами по оценке рассуждений. Исследователь, предложивший новую задачу, сможет вместо словесного описания методологии просто опубликовать файл experiment.yaml, сделав свою работу идеально воспроизводимой и расширяемой.

## Дорожная карта

Проект будет развиваться поэтапно, чтобы обеспечить стабильность и сфокусироваться на наиболее важных функциях:

- **Фаза 1: MVP (v0.2 — текущая)** — Потоковый режим, GGUF-менеджер, базовые метрики, CLI, движок выполнения YAML, адаптеры для transformers и Gemini, основные метрики и вывод в JSONL, полная система LLM-судей
- **Фаза 2: Версия 1.0** — Условная логика в YAML, расширенная библиотека метрик, загрузка данных из Hugging Face Datasets, генерация отчетов, кеширование, визуализации
- **Фаза 3: Будущее** — Публичный реестр задач на рассуждение, инструменты визуализации, поддержка мультимодальных задач, развитие сообщества, сообщество плагинов

## Участие в проекте

Мы приветствуем вклад в развитие AI-Reasoning-Lab! Наша плагинная архитектура позволяет легко добавлять новые адаптеры моделей и метрики, не изменяя ядро фреймворка. Плагины моделей и метрик подключаются без изменения ядра.

Если вы хотите внести свой вклад:

1. Откройте Issue с идеей
2. Создайте Fork → PR

## Лицензия

Этот проект лицензирован на условиях лицензии Apache 2.0.