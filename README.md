# AI-Reasoning-Lab
**AI-Reasoning-Lab — это фреймворк с открытым исходным кодом для проектирования, выполнения и анализа сложных, состоянийных, многошаговых задач на рассуждение с целью глубокой оценки когнитивных способностей продвинутых языковых моделей.**
--- 
## Проблема: "Слепое пятно" в оценке LLM
Современные инструменты оценки LLM, такие как `lm-evaluation-harness` или   
`OpenAI Evals`, отлично справляются с бенчмаркингом моделей на стандартных задачах. Однако они в основном оценивают точность   
**конечного ответа**. Это создает критическое "слепое пятно": модель может прийти к правильному выводу, используя ошибочную логику, или просто "угадать" ответ, и система оценки зафиксирует успех. Мы измеряем не истинную способность к рассуждению, а лишь сложную форму сопоставления с образцом.   
Для создания действительно надежных и интеллектуальных систем нам нужны инструменты, которые могут заглянуть "под капот" и оценить сам **процесс рассуждения**.
## Наше решение: Оценка процесса, а не только результата
AI-Reasoning-Lab смещает фокус с оценки конечного ответа на валидацию всей цепочки рассуждений. Вместо простых пар "промпт-ответ" наш фреймворк позволяет определять сложные, многошаговые задачи, где вывод одного шага становится входом для следующего. Это позволяет детально анализировать, как модель строит свою логику, выявлять ошибки в промежуточных шагах и по-настоящему измерять ее когнитивные способности.
### Ключевые особенности
- **Декларативные тесты в YAML:** Проектируйте сложные, многошаговые сценарии оценки в простых и читаемых файлах `experiment.yaml`. Никакого сложного кода — только логика вашего эксперимента.
- **Многошаговая и состоянийная оценка:** Создавайте задачи, где состояние (контекст) сохраняется и передается между шагами, имитируя реальные процессы решения проблем и Chain-of-Thought (CoT).
- **Оценка промежуточных шагов:** Применяйте метрики не только к финальному результату, а к выводу на любом шаге цепочки рассуждений.
- **Плагинная архитектура моделей:** Легко подключайте новые модели через адаптеры. MVP будет поддерживать локальные модели `transformers` и API (например, Gemini), а архитектура позволяет легко добавлять новые.
- **Расширяемая система метрик:** Используйте встроенные метрики ( `exact\_match`, `regex\_match`) или легко добавляйте свои собственные. Включает поддержку методологии "LLM-как-судья" для качественной оценки.
- **Детализированные результаты:** Получайте подробные логи каждого шага в формате JSONL, создавая полный "след доказательств" процесса рассуждения модели для последующего анализа.

Ниже представлена подробная информация о ключевых модулях, составляющих ядро проекта.
## 📚 Раздел base\_logic - Базовая Логическая Платформа
### Назначение и философия
**BaseLogic** представляет собой **фундаментальную тестовую платформу** для комплексного анализа и оценки языковых моделей. Модуль построен на принципах модульности, воспроизводимости и безопасности, обеспечивая надежную основу для всех типов тестирования.
### Архитектура: Четырехуровневая система
**Уровень 1: Утилиты и Ядро (Core & Utilities)**
```
baselogic/core/
├── config_loader.py     # Singleton управления конфигурацией
├── logger.py            # Централизованная система логирования
├── types.py             # Система типов и интерфейсов
├── enums.py             # Перечисления и константы
└── interfaces.py        # Контракты взаимодействия


```
**Уровень 2: Сервисный слой (Service Layer)**
```
├── client_factory.py    # Factory Pattern для LLM клиентов
├── llm_client.py        # Основной клиент LLM
├── base_client.py       # Базовая абстракция клиента
├── http_client.py       # HTTP клиент для API
└── safe_evaluator.py    # Безопасное выполнение кода


```
**Уровень 3: Бизнес-логика (Business Logic)**
```
├── test_runner.py       # Оркестратор тестирования (Strategy Pattern)
├── plugin_manager.py    # Система плагинов
├── progress_tracker.py  # Отслеживание прогресса
├── metrics.py           # Система метрик и статистики
└── config_validator.py  # Валидация конфигурации


```
**Уровень 4: Отчетность и анализ (Reporting Layer)**
```
├── reporter.py          # Базовый генератор отчетов с Wilson Score
├── advanced_reporter.py # Продвинутая аналитика и сегментация
└── report_formatter.py  # Форматирование вывода


```
### Ключевые компоненты BaseLogic
**1. Система тестирования (Test Framework)**
- **AbstractTestGenerator**: Базовый контракт для всех тестов
- **Strategy Pattern**: Динамическое подключение различных типов тестов
- **Plugin System**: Расширяемость через пользовательские тесты

**2. Система метрик (Metrics System)**
- **Wilson Score Algorithm**: Статистически корректный расчет Trust Score
- **Verbosity Index**: Анализ "болтливости" моделей
- **Performance Tracking**: Время выполнения, точность, надежность

**3. Система безопасности (Security Framework)**
- **SafeExpressionEvaluator**: Безопасное выполнение математических выражений
- **Input Validation**: Валидация всех входных данных
- **Sandboxing**: Изоляция выполнения тестов

**4. Конфигурационная система**
- **Environment Variables**: Гибкая настройка через.env файлы
- **Multiple Providers**: Поддержка Ollama, OpenAI-compatible APIs
- **Dynamic Configuration**: Изменение параметров во время выполнения

### Тестовые категории BaseLogic
**Уровень 1: Базовый контроль**   
Python
```
# t01_simple_logic.py - Простые дедуктивные задачи
def generate(self) -> Dict[str, Any]:
    return {
        'prompt': 'Анна выше Бориса. Борис выше Веры. Кто самый высокий?',
        'expected_output': 'Анна'
    }

# t02_instructions.py - Точное следование инструкциям
def generate(self) -> Dict[str, Any]:
    instructions = [
        "1. Возьми исходную фразу: 'мама мыла раму'",
        "2. Напиши ее в ВЕРХНЕМ РЕГИСТРЕ",
        "3. Посчитай количество гласных букв",
        "4. Выведи в формате: ОБРАБОТАНО: [фраза], ГЛАСНЫХ: [число]"
    ]

# t06_mathematics.py - Арифметические вычисления
def generate(self) -> Dict[str, Any]:
    expression = f"({num1} {op1} {num2}) {op2} {num3}"
    expected_result = SafeExpressionEvaluator.evaluate(expression)


```
 --- 
## 🏆 Раздел grandmaster - Генератор Логических Головоломок
### Назначение и инновация
**Grandmaster** - это **продвинутый процедурный генератор логических головоломок экспертного уровня**, разработанный для создания уникальных задач, которые невозможно найти в обучающих данных LLM. Это ключевое решение проблемы "data contamination" в современных бенчмарках.
### Алгоритм "Гроссмейстер-Виртуоз"
**Трехфазный процесс целенаправленного проектирования головоломок:**   
**Фаза 1: Проектирование "Каркаса Интриги"**   
Python
```
def create_intrigue_framework(self):
    """
    Принцип: "Начни со сложного и разнообразного"

    Процесс:
    - Выбор "экзотических" улик из сложных типов
    - Размещение в ядре головоломки
    - Дополнение другими сложными уликами

    Результат: Неоднозначный каркас, требующий нелинейного мышления
    """
    exotic_clues = [
        'три_в_ряд',      # Структурные паттерны
        'сумма_позиций',  # Арифметические связи
        'либо_либо',      # Дизъюнктивная логика
        'условное_если'   # Импликации
    ]
    return self.build_core_framework(exotic_clues)


```
**Фаза 2: Интеллектуальное Укрепление**   
Python
```
def intelligent_reinforcement(self, framework):
    """
    Принцип: "Используй грубую силу только в крайнем случае"

    Процесс:
    - Итеративное добавление только сложных улик
    - Постоянная проверка на противоречивость
    - Простые факты - только при крайней необходимости

    Результат: Головоломка на сложных и косвенных уликах
    """
    complex_clue_types = ['косвенные', 'негативные', 'условные']

    while not self.is_complete(framework):
        clue = self.select_complex_clue(complex_clue_types)
        if self.is_consistent(framework + [clue]):
            framework.append(clue)

    return framework


```
**Фаза 3: Шлифовка и Финальный Аудит**   
Python
```
def polish_and_audit(self, puzzle):
    """
    Принцип: "Отсеки лишнее и проверь на глубину"

    Процесс:
    1. Пакетная минимизация - удаление сотен избыточных улик
    2. Аудит качества - построение графа связей
    3. Поиск самого длинного логического пути

    Результат: Элегантная головоломка с гарантированно глубоким решением
    """
    # Пакетная минимизация
    puzzle = self.batch_minimize(puzzle)

    # Аудит качества
    logic_graph = self.build_logic_graph(puzzle)
    deepest_question = self.find_longest_path(logic_graph)

    return {
        'puzzle': puzzle,
        'solution_depth': len(deepest_question),
        'elegance_score': self.calculate_elegance(puzzle)
    }


```
### Технические возможности Grandmaster
**1. Масштабируемая сложность**
- Генерация задач от 4x4 до 8x8
- Динамическая настройка уровня сложности
- Адаптация к возможностям целевой модели

**2. Промышленный движок**
- **Google OR-Tools (CP-SAT Solver)** для максимальной производительности
- Поддержка тысяч "ветвлений" в решении
- Гарантированная единственность решения

**3. Богатый репертуар подсказок**
- **Условные**: "Если A, то B"
- **Дизъюнктивные**: "Либо A, либо B"
- **Эквивалентные**: "A тогда и только тогда, когда B"
- **Структурные**: "Три в ряд", "Сумма позиций"
- **Пространственные**: "Рядом с", "Между"

**4. Естественный язык**   
JSON
```
// linguistics.json - Грамматически верные подсказки
{
  "templates": {
    "implication": [
      "Если {subject} {predicate}, то {conclusion}",
      "В случае когда {condition}, обязательно {result}"
    ],
    "exclusion": [
      "{item1} и {item2} не могут быть {attribute} одновременно",
      "Либо {option1}, либо {option2}, но не оба"
    ]
  }
}


```
### Интеграция с BaseLogic
**Процедурная стратегия тестирования:**   
Python
```
class ProceduralLogicTest(AbstractTestGenerator):
    def __init__(self, test_id: str):
        super().__init__(test_id)
        self.grandmaster = Grandmaster(
            grid_size=6,
            num_categories=5,
            complexity_level='expert'
        )

    def generate(self) -> Dict[str, Any]:
        """Генерирует уникальную головоломку на лету"""
        puzzle = self.grandmaster.generate_puzzle()

        return {
            'prompt': puzzle.to_natural_language(),
            'expected_output': puzzle.solution,
            'metadata': {
                'solution_depth': puzzle.solution_depth,
                'elegance_score': puzzle.elegance_score,
                'generation_seed': puzzle.seed
            }
        }

    def verify(self, llm_output: str, expected_output: Any) -> Dict[str, Any]:
        """Верификация структурированного ответа"""
        extracted_solution = self.parse_llm_solution(llm_output)
        is_correct = self.compare_solutions(extracted_solution, expected_output)

        return {
            'is_correct': is_correct,
            'details': {
                'extracted_solution': extracted_solution,
                'expected_solution': expected_output,
                'parsing_success': extracted_solution is not None
            }
        }


```
### Уникальные преимущества Grandmaster
**1. Защита от Data Contamination**
- Каждая головоломка генерируется уникально
- Невозможность "заучивания" решений
- Истинная оценка когнитивных способностей

**2. Масштабируемая сложность**
- От простых 4x4 для быстрой оценки
- До экспертных 8x8 для stress-тестирования
- Адаптивная сложность под целевую модель

**3. Методологическая строгость**
- Гарантированная разрешимость
- Единственность решения
- Воспроизводимость через seed

**4. Исследовательская ценность**
- Выявление пределов рассуждения у SOTA моделей
- Фундамент для "Теста Тьюринга 2.0"
- База для исследования AGI-способностей
 --- 

## 🔗 Синергия модулей
**BaseLogic** обеспечивает **надежную инфраструктуру** для тестирования, а **Grandmaster** предоставляет **уникальный контент** высочайшего качества. Вместе они формируют **мощный инструмент для честной и глубокой оценки** современных систем искусственного интеллекта, защищенный от основных угроз достоверности бенчмарков.   
Эта комбинация делает AI-Reasoning-Lab **уникальной платформой** в ландшафте оценки ИИ, способной не только тестировать текущие модели, но и расти вместе с развитием технологий.
## Начало работы
### 1. Установка
Bash
```
pip install ai-reasoning-lab


```
### 2. Создайте свой первый эксперимент
Создайте файл `experiment.yaml`. Вот пример, который проверяет способность модели решить простую логическую головоломку в два шага: сначала рассуждение, затем вывод.   
YAML
```
version: 0.1.0
experiment_id: simple_logic_puzzle
model:
  provider: huggingface
  name: google/flan-t5-large
tasks:
  - task_id: puzzle_1
    steps:
      - step_id: step1_reason
        prompt_template: "Реши эту логическую головоломку по шагам: Если А старше Б, а Б старше В, кто самый старший? Подумай вслух."
        evaluations:
          - metric: contains_all
            params:
              substrings: ["А старше Б", "Б старше В"]
      - step_id: step2_conclude
        prompt_template: "{{ steps.step1_reason.output }}\n\nОсновываясь на приведенных выше рассуждениях, какой окончательный ответ? Назови только имя."
        evaluations:
          - metric: exact_match
            ground_truth: "А"


```
### 3. Запустите эксперимент
Используйте наш CLI для запуска эксперимента.   
Bash
```
arl run experiment.yaml --output results.jsonl


```
### 4. Проанализируйте результаты
Результаты будут сохранены в `results.jsonl`. Каждая строка в файле представляет собой результат одного шага, что позволяет детально изучить весь процесс.   
**Пример строки вывода:**   
JSON
```
{
  "experiment_id": "simple_logic_puzzle",
  "task_id": "puzzle_1",
  "step_id": "step2_conclude",
  "timestamp": "2024-09-21T14:30:00Z",
  "prompt": "...",
  "response": "А",
  "evaluations": [
    {
      "metric": "exact_match",
      "ground_truth": "А",
      "result": {"score": 1.0, "match": true}
    }
  ],
  "metadata": {"model": "google/flan-t5-large", "latency_ms": 540}
}


```
## Основная философия
Мы считаем, что будущее оценки LLM — не в создании все более крупных монолитных бенчмарков, а в предоставлении исследователям и разработчикам мощных инструментов для создания собственных, предметно-ориентированных тестов.   
Наша цель — сделать `experiment.yaml` универсальным языком (*lingua franca*) для описания и обмена сложными экспериментами по оценке рассуждений. Исследователь, предложивший новую задачу, сможет вместо словесного описания методологии просто опубликовать файл `experiment.yaml`, сделав свою работу идеально воспроизводимой и расширяемой.
## Дорожная карта
Проект будет развиваться поэтапно, чтобы обеспечить стабильность и сфокусироваться на наиболее важных функциях.
- **Фаза 1: MVP (Текущая)**
    - Базовый CLI, движок выполнения YAML, адаптеры для `transformers` и Gemini, основные метрики и вывод в JSONL.
- **Фаза 2: Версия 1.0**
    - Условная логика в YAML, расширенная библиотека метрик, загрузка данных из Hugging Face Datasets, генерация отчетов и кеширование.
- **Фаза 3: Будущее**
    - Публичный реестр задач на рассуждение, инструменты визуализации, поддержка мультимодальных задач и развитие сообщества.

## Участие в проекте
Мы приветствуем вклад в развитие AI-Reasoning-Lab! Наша плагинная архитектура  позволяет легко добавлять новые адаптеры моделей и метрики, не изменяя ядро фреймворка.   
Если вы хотите внести свой вклад, пожалуйста, ознакомьтесь с `CONTRIBUTING.md` (будет добавлено в ближайшее время). Вы можете начать с создания Issue для обсуждения вашей идеи.
## Лицензия
Этот проект лицензирован на условиях лицензии Apache 2.0.   
