# Генератор Логических Головоломок (Архитектура v5.0)

## Введение

Этот проект представляет собой Python-скрипт для процедурной генерации логических головоломок в стиле "Загадки Эйнштейна" (Zebra Puzzle). Его основная цель — создавать сложные, но гарантированно решаемые задачи для тестирования и оценки способностей к логическому выводу и рассуждению у больших языковых моделей (LLM).

В отличие от многих других генераторов, этот проект фокусируется на создании головоломок, которые **невозможно решить поверхностным сопоставлением ключевых слов**. Он заставляет решателя (человека или ИИ) выстраивать сложную внутреннюю модель мира, делать выводы из косвенных улик и даже реконструировать скрытые правила самой игры.

## 🚀 Ключевые Возможности

*   **Настраиваемая Сложность:** Генерация головоломок от 1 до 10 уровня сложности с увеличением размера сетки и качества подсказок.
*   **Тематическая Гибкость:** Четыре встроенные темы (Офис, Квартал, Киберпанк, Стимпанк) и простая архитектура для добавления новых.
*   **Гарантированная Решаемость:** Каждая головоломка проверяется встроенным решателем (`Solver`), что гарантирует наличие единственного уникального решения.
*   **Минимальный Набор Подсказок:** После генерации решаемого набора подсказок, алгоритм удаляет все избыточные улики, чтобы сделать задачу максимально лаконичной и сложной.
*   **Продвинутые Типы Ограничений:** Используются условные, пространственные и сложные транзитивные подсказки, требующие многоуровневых рассуждений.
*   **Интеллектуальный Алгоритм Построения:** Генератор использует "архитектурные планы" и приоритетный подбор подсказок, чтобы **принудить** решателя использовать сложную логику, а не находить простые обходные пути.

## 🧠 Как это работает: Принуждение к Сложной Логике

Сложность головоломки определяется не только размером сетки, но и качеством самих подсказок. Генератор использует несколько уровней усложнения, чтобы протестировать различные аспекты мышления ИИ.

### Уровни 1-6: Базовая Дедукция

*   **Структура:** Простая линейная геометрия (элементы расположены в ряд от 1 до N).
*   **Подсказки:** В основном используются прямые (`Характеристикой А является Б`) и отрицательные (`В НЕ находится в одной локации с Г`) связи. Это проверяет базовую способность LLM к сопоставлению и исключению.

### Уровни 7-8: Скрытая Круговая Геометрия

*   **Структура:** **Круговая геометрия.** Локации замыкаются в кольцо. Это фундаментальное изменение, так как исчезают "края" (первая и последняя позиция), которые служили легкими точками для старта решения.
*   **Подсказки:** В "рецепт" генерации **обязательно** включаются:
    *   **Условные (`Если... то...`):** Требуют применения логических правил и их контрапозиции.
    *   **Пространственные (`...слева от...`):** Описывают взаимное расположение элементов.
    *   **"Напротив":** "Сотрудник IT и тот, кто пьет эспрессо, находятся в локациях друг напротив друга". Эта подсказка бессмысленна в линейной структуре и служит ключевым намеком на геометрию задачи.

### Уровни 9-10: Абдуктивное Мышление и Комплексные Ограничения

Это вершина сложности, разработанная для того, чтобы "сломать" модели, которые полагаются на поиск "коротких путей". Здесь LLM должна не просто решить головоломку, а **сначала вывести правила самой игры**.

1.  **Полностью Скрытые Правила:**
    *   Из текста задачи **полностью убраны** упоминания "круговой структуры" и направления "по часовой стрелке".
    *   Модель должна сама прийти к выводу о геометрии задачи, проанализировав совокупность подсказок. Это тест на **абдуктивное мышление** — способность находить наилучшее объяснение для набора наблюдений.

2.  **Сложная Транзитивная Пространственная Подсказка:**
    *   Это самый коварный тип подсказки, например: *"Проект "Титан" находится в локации между той, где работает сотрудник Волков, и той, где пьют зеленый чай"*.
    *   **Почему это так сложно для ИИ:** Она связывает три сущности из **трех разных категорий** в **жесткий пространственный порядок**, разрушая простую попарную логику.

3.  **Принуждение к Сложной Логике (Архитектура v5.0):**
    *   **Проблема:** Предыдущие версии иногда создавали задачи, где сложные подсказки были "шумом", а ответ можно было найти через простые прямые связи.
    *   **Решение:** Алгоритм генерации был полностью переработан. Для уровней 9-10 **из "рецепта" полностью исключены "дешёвые" прямые подсказки**. Если для решения не хватает "скелета" из сложных улик, генератор начинает добавлять новые в строгом порядке приоритета: сначала отрицания, потом пространственные связи, и только в самом крайнем случае — прямые. Это гарантирует, что ядро решения лежит именно в сложной, а не в простой логике.

## 🛠️ Использование

### Требования
*   Python 3.8+
*   Библиотека `pandas`

### Установка
1.  Клонируйте репозиторий:
    ```bash
    git clone <URL-репозитория>
    cd <имя-папки>
    ```
2.  Установите зависимость:
    ```bash
    pip install pandas
    ```

### Запуск генерации
Для генерации головоломки просто запустите скрипт. Основная логика находится в блоке `if __name__ == '__main__':`.

```python
# В конце файла LogicPuzzleGenerator.py

if __name__ == '__main__':
    # ... (определение тем и элементов) ...
    
    generator = ElegantLogicPuzzleGenerator(themes=THEMES, story_elements=puzzle_story_elements)
    
    # Измените значение difficulty для генерации задачи нужного уровня
    print("--- ГЕНЕРАЦИЯ ЭКСПЕРТНОЙ ЗАДАЧИ (АРХИТЕКТУРА v5.0) ---")
    generator.generate(difficulty=10, verbose_solver=False)
```

Вы можете изменить параметры вызова `generator.generate()`:
*   `difficulty` (от 1 до 10): Уровень сложности головоломки.
*   `verbose_solver` (True/False): Если `True`, в консоль будет выведено скрытое решение (таблица) для самопроверки.
*   `log_file_path` (str): Путь к файлу для записи подробного лога работы внутреннего решателя.

## 🏗️ Структура Кода

Проект состоит из двух основных классов:

*   `ElegantLogicPuzzleGenerator`: "Архитектор". Этот класс отвечает за:
    1.  Выбор темы и данных в соответствии с уровнем сложности.
    2.  Создание случайного, но валидного решения.
    3.  Генерацию пула подсказок, разделенного по типам.
    4.  Построение "скелета" головоломки по "архитектурному плану" (`target_counts`).
    5.  **Приоритетное достраивание:** Если "скелета" недостаточно, генератор добавляет подсказки в строгом порядке (`negative_direct_link` -> `relative_pos` -> ...), чтобы гарантировать сложную логику.
    6.  Минимизацию набора подсказок путем удаления избыточных.
    7.  Форматирование и вывод готовой задачи.

*   `ConstraintSatisfactionSolver`: "Верификатор". Это решатель, основанный на алгоритме распространения ограничений (AC-3-like). Он используется генератором на каждом шаге, чтобы проверить, решена ли головоломка полностью. Ключевая особенность — способность "распаковывать" сложные подсказки в набор более простых, что позволяет сохранить высокую производительность.

## 🎨 Кастомизация и Расширение

Вы легко можете добавить свои собственные темы и сценарии.

1.  **Добавление темы:** Откройте скрипт и найдите словарь `THEMES`. Добавьте новый ключ с названием вашей темы и вложенный словарь с категориями и списками их значений. Убедитесь, что для каждой категории достаточно уникальных элементов.

    ```python
    THEMES = {
        # ... существующие темы ...
        "ВАША НОВАЯ ТЕМА": {
            "Категория 1": ["Значение1", "Значение2", ...],
            "Категория 2": ["ЗначениеA", "ЗначениеB", ...],
        }
    }
    ```

2.  **Настройка текста:** В словаре `puzzle_story_elements` можно изменить то, как названия категорий будут отображаться в тексте подсказок.

    ```python
    puzzle_story_elements = {
        # ... существующие элементы ...
        "Категория 1": "сущность типа 1",
        "Категория 2": "предмет типа 2",
    }
    ```

## 🔮 Будущие Идеи

*   **Динамические подсказки:** Введение подсказок, основанных на атрибутах, а не на конкретных значениях ("Локация с самым высоким номером этажа находится рядом с той, где пьют самый горячий напиток").
*   **Неопределенность:** Добавление подсказок с некоторой долей неопределенности или "красных сельдей" для проверки способности ИИ отфильтровывать нерелевантную информацию.
*   **Автоматическая оценка:** Интеграция с API языковых моделей для автоматического запуска тестов и сбора статистики по решаемости на разных уровнях сложности.

