# Генератор Логических Головоломок: Бенчмарк для Тестирования Рассуждений ИИ

Этот проект представляет собой Python-скрипт для процедурной генерации логических головоломок в стиле "Загадки Эйнштейна" (Zebra Puzzle). Его основная цель — создавать сложные, но гарантированно решаемые задачи для тестирования и оценки способностей к логическому выводу и рассуждению у больших языковых моделей (LLM).

Генератор способен создавать головоломки с настраиваемым уровнем сложности, от простых задач для новичков до "непробиваемых" экспертных уровней, которые требуют от решателя не просто следовать правилам, а реконструировать скрытые правила самой игры.

## 🚀 Ключевые Возможности

*   **Настраиваемая Сложность:** Генерация головоломок от 1 до 10 уровня сложности с увеличением количества элементов, категорий и типов подсказок.
*   **Тематическая Гибкость:** Легкая настройка и добавление новых тем (офисные тайны, киберпанк-детектив, стимпанк-алхимия и т.д.) для разнообразия сценариев.
*   **Гарантированная Решаемость:** Каждая сгенерированная головоломка проходит проверку встроенным решателем (`Solver`), что гарантирует наличие единственного уникального решения.
*   **Минимальный Набор Подсказок:** После нахождения решаемого набора подсказок, генератор проходит этап "очистки", удаляя все избыточные подсказки, чтобы сделать задачу максимально лаконичной и сложной.
*   **Продвинутые Типы Ограничений:** Используются не только прямые и отрицательные связи, но и условные, пространственные и сложные транзитивные подсказки, требующие многоуровневых рассуждений.
*   **Чистый и Расширяемый Код:** Проект написан на современном Python (3.8+) с аннотациями типов, подробными комментариями и четким разделением на классы `ElegantLogicPuzzleGenerator` и `ConstraintSatisfactionSolver`.

## 🧠 Как это работает: Уровни сложности и "ловушки" для ИИ

Сложность головоломки определяется не только размером сетки, но и качеством самих подсказок. Генератор использует несколько уровней усложнения, чтобы протестировать различные аспекты мышления ИИ.

### Уровни 1-6: Базовая Дедукция

*   **Структура:** Простая линейная геометрия (элементы расположены в ряд от 1 до N).
*   **Подсказки:** В основном используются прямые (`А связано с Б`) и отрицательные (`В НЕ связано с Г`) связи. Это проверяет базовую способность LLM к сопоставлению и исключению.
*   **Цель:** Убедиться, что модель может выполнять простые дедуктивные шаги и заполнять таблицу на основе прямых фактов.

### Уровни 7-8: Скрытая Геометрия и Условная Логика

*   **Структура:** **Круговая геометрия.** Локации замыкаются в кольцо (за последней следует первая). Это фундаментальное изменение, так как исчезают "края" (первая и последняя позиция), которые служили легкими точками для старта решения.
*   **Подсказки:**
    *   **Условные (`Если... то...`):** "Если в локации находится инженер, то там же пьют кофе". Это требует от модели не простого сопоставления, а применения логических правил и их контрапозиции ("Если в локации НЕ пьют кофе, то там НЕ может быть инженера").
    *   **Пространственные (`...слева от...`):** Вводятся подсказки, описывающие взаимное расположение элементов.

### Уровни 9-10: Абдуктивное Мышление и Комплексные Ограничения

Это вершина сложности, разработанная для того, чтобы "сломать" модели, которые полагаются только на поверхностное сопоставление текста. Здесь LLM должна не просто решить головоломку, а **сначала вывести правила самой игры**.

1.  **Полностью Скрытые Правила:**
    *   Из текста задачи **полностью убраны** упоминания "круговой структуры" и направления "по часовой стрелке".
    *   Модель должна сама прийти к выводу о геометрии задачи, проанализировав совокупность подсказок. Если каждая локация имеет соседа слева и справа, а также существует понятие "напротив", единственным логичным выводом является круговая структура. Это тест на **абдуктивное мышление** — способность находить наилучшее объяснение для набора наблюдений.

2.  **Подсказка "Напротив":**
    *   "Сотрудник в отделе IT и тот, кто пьет эспрессо, находятся в локациях друг напротив друга".
    *   Эта подсказка создает жесткую глобальную связь между двумя элементами на максимальном удалении друг от друга. Она бессмысленна в линейной структуре и является еще одним ключом к пониманию круговой геометрии.

3.  **Сложная Транзитивная Пространственная Подсказка:**
    *   Это самый коварный тип подсказки, например: *"Проект "Титан" находится в локации между той, где работает сотрудник Волков, и той, где пьют зеленый чай"*.
    *   **Почему это так сложно для ИИ:**
        *   **Три разные категории:** Подсказка связывает три сущности из трех разных категорий (`Проект`, `Сотрудник`, `Напиток`).
        *   **Жесткий порядок:** Она накладывает строгий пространственный порядок: `Волков -> Титан -> Зеленый чай`.
        *   **Разрушение попарной логики:** Модель не может решить это, просто сопоставив две сущности. Она вынуждена удерживать в контексте сложную тройную связь и проверять гипотезы о расположении сразу трех элементов.

## 🛠️ Использование

### Требования
*   Python 3.8+
*   Библиотека `pandas`

### Установка
1.  Клонируйте репозиторий:
    ```bash
    git clone <URL-репозитория>
    cd <имя-папки>
    ```
2.  Установите зависимость:
    ```bash
    pip install pandas
    ```

### Запуск генерации
Для генерации головоломки просто запустите скрипт. Основная логика находится в блоке `if __name__ == '__main__':`.

```python
# В конце файла LogicPuzzleGenerator.py

if __name__ == '__main__':
    # ... (определение тем и элементов) ...
    
    generator = ElegantLogicPuzzleGenerator(themes=THEMES, story_elements=puzzle_story_elements)
    
    # Измените значение difficulty для генерации задачи нужного уровня
    print("--- ГЕНЕРАЦИЯ ЭКСПЕРТНОЙ ЗАДАЧИ ---")
    generator.generate(difficulty=10, verbose_solver=False)
```

Вы можете изменить параметры вызова `generator.generate()`:
*   `difficulty` (от 1 до 10): Уровень сложности головоломки.
*   `verbose_solver` (True/False): Если `True`, в консоль будет выведено скрытое решение (таблица) для самопроверки.
*   `log_file_path` (str): Путь к файлу для записи подробного лога работы внутреннего решателя. Полезно для отладки.

## 🏗️ Структура Кода

Проект состоит из двух основных классов:

*   `ElegantLogicPuzzleGenerator`: "Архитектор". Этот класс отвечает за:
    1.  Выбор темы и данных в соответствии с уровнем сложности.
    2.  Создание случайного, но валидного решения.
    3.  Генерацию полного пула всех возможных подсказок на основе этого решения.
    4.  Построение головоломки по "архитектурному плану" (`target_counts`), выбирая определенное количество подсказок каждого типа.
    5.  Добавление подсказок до тех пор, пока головоломка не станет решаемой.
    6.  Минимизацию набора подсказок путем удаления избыточных.
    7.  Форматирование и вывод готовой задачи.

*   `ConstraintSatisfactionSolver`: "Верификатор". Это решатель, основанный на алгоритме распространения ограничений (AC-3-like). Он используется генератором на каждом шаге, чтобы проверить:
    1.  Не противоречива ли головоломка.
    2.  Решена ли она полностью.

    Ключевая особенность решателя — способность "распаковывать" сложные подсказки (например, транзитивные) в набор более простых, что позволяет сохранить высокую производительность.

## 🎨 Кастомизация и Расширение

Вы легко можете добавить свои собственные темы и сценарии.

1.  **Добавление темы:** Откройте скрипт и найдите словарь `THEMES`. Добавьте новый ключ с названием вашей темы и вложенный словарь с категориями и списками их значений.

    ```python
    THEMES = {
        "Офисная Тайна": { ... },
        "Загадка Тихого Квартала": { ... },
        "ВАША НОВАЯ ТЕМА": {
            "Категория 1": ["Значение1", "Значение2", ...],
            "Категория 2": ["ЗначениеA", "ЗначениеB", ...],
            # ... и так далее
        }
    }
    ```

2.  **Настройка текста:** В словаре `puzzle_story_elements` можно изменить то, как названия категорий будут отображаться в тексте подсказок.

    ```python
    puzzle_story_elements = {
        "scenario": "", "position": "локация",
        "Категория 1": "сущность типа 1",
        "Категория 2": "предмет типа 2",
    }
    ```

## 🔮 Будущие Идеи

*   **Динамические подсказки:** Введение подсказок, основанных на атрибутах, а не на конкретных значениях ("Локация с самым высоким номером этажа находится рядом с той, где пьют самый горячий напиток").
*   **Неопределенность:** Добавление подсказок с некоторой долей неопределенности или "красных сельдей" для проверки способности ИИ отфильтровывать нерелевантную информацию.
*   **Автоматическая оценка:** Интеграция с API языковых моделей для автоматического запуска тестов и сбора статистики по решаемости на разных уровнях сложности.

